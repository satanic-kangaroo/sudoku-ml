# 🎯 سودوکو ML — وظایف و نقشه‌راه

> **فلسفه:** کم بساز، زیاد تحویل بده. هر بار فقط یک اسپرینت فعال.
> هر وظیفه یک چک‌باکس دارد و جزئیات کافی برای اینکه ماه‌ها بعد هم بتوانی ادامه‌اش بدهی.

**آخرین به‌روزرسانی:** ۲۰۲۶-۰۹-۲۳
**اسپرینت فعلی:** 🎯 اسپرینت ۱ — بردهای سریع

---

## 📊 راهنما

| نماد | معنی |
|--------|---------|
| 🎯 | اسپرینت فعال |
| ✅ | انجام‌شده |
| ⏳ | در حال انجام |
| 🧊 | یخچال (برنامه‌ریزی‌نشده) |
| ⭐ | تأثیر بالا |
| ⚠️ | مسدود / نیاز به تصمیم |
| 🔗 | وابسته به وظیفه‌ای دیگر |

---

# 🎯 اسپرینت ۱ — بردهای سریع

**مدت:** ۱ روز
**هدف:** صیقل‌دادن UX بدون هیچ ریسک معماری.

---

## ✅ #4 کپی تخته به‌صورت متن

**چرا:** کاربران اغلب می‌خواهند پازل را در یک حل‌کننده، صفحه‌گسترده یا اپ یادداشت پیست کنند.

**فایل‌هایی که باید تغییر کنند:**
- `sudoku_solver/pipeline.py` — افزودن تابع کمکی `board_to_text()`
- `app.py` — افزودن دکمه‌ی «Copy as text» نزدیک تب حل

**جزئیات پیاده‌سازی:**
- [ ] ساخت `board_to_text(board, style="dots")` در `sudoku_solver/pipeline.py`
  - سبک‌هایی که باید پشتیبانی شوند:
    - `"dots"` → `5 3 . . 7 . . . .`
    - `"zeros"` → `5 3 0 0 7 0 0 0 0`
    - `"grid"` → ASCII با کادر (منطق `_print_board` از قبل موجود است)
- [ ] افزودن `board_to_text(board, style="compact")` — قالب تک‌خطی مثل `530070000600195000...`
- [ ] در `app.py`، افزودن یک بخش «Copy as text» زیر تب **Solution**
  - [ ] سه بلوک `st.code()` با قالب‌های مختلف
  - [ ] هر کدام دکمه‌ی کپی مخصوص خودش را دارد (دکمه‌ی کپی داخلی `st.code` در Streamlit)
- [ ] افزودن یک یادداشت کوچک: «روی آیکن کپی در گوشه‌ی بالا-راست کلیک کن»

**معیارهای پذیرش:**
- کلیک روی هر دکمه‌ی کپی، قالب درست را کپی می‌کند
- هر سه قالب با خانه‌های خالی به‌شکل یکسان رفتار می‌کنند
- روی موبایل کار می‌کند (کپی بومی Streamlit روی Safari/Chrome موبایل کار می‌کند)

---

## ✅ #5 میان‌بر صفحه‌کلید Ctrl+Enter برای حل

**چرا:** صرفه‌جویی در یک کلیک هنگام کار روی تصاویر مختلف.

**فایل‌هایی که باید تغییر کنند:**
- `app.py` — تزریق یک JS کوچک از طریق `st.markdown`

**جزئیات پیاده‌سازی:**
- [ ] افزودن یک قطعه‌ی JS که به `Ctrl+Enter` گوش بدهد و دکمه‌ی اصلی را کلیک کند
- [ ] محافظ: فقط زمانی فعال شود که دکمه‌ی «Solve» در صفحه وجود دارد
- [ ] افزودن tooltip روی دکمه: «Ctrl+Enter to solve»
- [ ] تست روی:
  - [ ] Chrome دسکتاپ
  - [ ] Firefox دسکتاپ
  - [ ] Safari دسکتاپ
  - [ ] موبایل (صفحه‌کلید ندارد، پس بی‌اثر است)

**قطعه‌کد مرجع:**
```js
document.addEventListener('keydown', (e) => {
  if ((e.ctrlKey || e.metaKey) && e.key === 'Enter') {
    const btn = [...document.querySelectorAll('button')]
      .find(b => b.innerText.includes('Solve puzzle'));
    btn?.click();
  }
});
```

**معیارهای پذیرش:**
- هم `Ctrl+Enter` (ویندوز/لینوکس) و هم `Cmd+Enter` (مک) کار می‌کنند
- هنگام تایپ در فیلد متنی، اشتباهاً فعال نمی‌شود
- دکمه همچنان با کلیک ماوس به‌طور عادی کار می‌کند

---

## ✅ #6 تولتیپ‌های توضیحی

**چرا:** تبدیل معیارهای مبهم به یک فرصت یادگیری.

**فایل‌هایی که باید تغییر کنند:**
- `sudoku_solver/styles.py` — CSS تولتیپ
- `app.py` — پیچیدن برچسب‌ها با HTML تولتیپ
- `sudoku_solver/glossary.py` *(جدید)* — تعاریف متمرکز

**جزئیات پیاده‌سازی:**
- [ ] ساخت `sudoku_solver/glossary.py` با یک دیکشنری:
  ```python
  GLOSSARY = {
      "confidence_threshold": {
          "short": "حداقل اطمینان مدل",
          "long": "ارقامی که اطمینانشان پایین‌تر از این مقدار باشد نادیده گرفته می‌شوند. "
                  "بالاتر = سخت‌گیرتر (ممکن است ارقام از دست بروند). پایین‌تر = آزادتر "
                  "(ممکن است نویز پذیرفته شود).",
          "range": "0.00 – 0.99",
          "default": 0.70,
      },
      "nodes_explored": {
          "short": "فراخوانی‌های بازگشتی انجام‌شده",
          "long": "هر بار که حل‌کننده یک نقطه‌ی تصمیم جدید را بررسی می‌کند، "
                  "این شمارنده یک واحد افزایش می‌یابد. گره‌های کمتر = کارآمدتر.",
      },
      "backtracks": {
          "short": "بن‌بست‌های برگردانده‌شده",
          "long": "وقتی حل‌کننده به بن‌بست می‌خورد و باید یک انتخاب را لغو کند، "
                  "این شمارنده افزایش می‌یابد. بازگشت زیاد = heuristic ضعیف.",
      },
      "mrv": {
          "short": "Minimum Remaining Values",
          "long": "یک heuristic: همیشه ابتدا محدودترین متغیر را امتحان کن. "
                  "در DLX یعنی ستونی با کمترین کاندیدا انتخاب شود.",
      },
      "dlx": {
          "short": "Dancing Links / Algorithm X",
          "long": "الگوریتم نات برای مسئله‌ی Exact Cover. از یک لیست پیوندی دایره‌ای "
                  "دوبعدی استفاده می‌کند که در آن cover/uncover از مرتبه‌ی O(1) است.",
      },
      "exact_cover": {
          "short": "هر قید دقیقاً یک بار",
          "long": "با داشتن یک ماتریس دودویی، سطرهایی را انتخاب کن که هر ستون "
                  "دقیقاً توسط یک سطر انتخاب‌شده پوشش داده شود.",
      },
      # ... برای همه‌ی معیارها ادامه بده
  }
  ```
- [ ] افزودن CSS برای `.tooltip` و `.tooltip:hover .tooltip-text`
- [ ] افزودن تابع کمکی `tip(key)` در `sudoku_solver/styles.py` که HTML برگرداند
- [ ] پیچیدن این برچسب‌ها با `tip(...)`:
  - [ ] آستانه‌ی اطمینان (نوار کناری)
  - [ ] گره‌های بررسی‌شده (جدول آمار)
  - [ ] بازگشت‌ها (جدول آمار)
  - [ ] heuristic MRV (بخش مقایسه)
  - [ ] الگوریتم حل‌کننده (radio)
  - [ ] نشان سختی
- [ ] افزودن یک expander «📖 واژه‌نامه» در پایین صفحه با فهرست همه‌ی اصطلاحات

**معیارهای پذیرش:**
- با نگه‌داشتن ماوس روی هر تولتیپ، توضیح نمایش داده می‌شود
- روی تم روشن و تیره هر دو کار می‌کند
- موبایل: نگه‌داشتن با انگشت، تولتیپ را نشان می‌دهد

---

## ✅ #3 تاریخچه / لاگ جلسه

**چرا:** کاربران اغلب چندین پازل را حل می‌کنند؛ یک لاگ اجازه‌ی مقایسه و مرور دوباره را می‌دهد.

**فایل‌هایی که باید تغییر کنند:**
- `app.py` — بخش نوار کناری + مدیریت state
- `sudoku_solver/history.py` *(جدید)* — مدل داده‌ی تاریخچه

**جزئیات پیاده‌سازی:**
- [ ] ساخت `sudoku_solver/history.py`:
  ```python
  from dataclasses import dataclass, field
  from datetime import datetime
  from typing import Optional
  import numpy as np

  @dataclass
  class SolveRecord:
      id: str                       # uuid کوتاه
      timestamp: datetime
      board: np.ndarray             # ۹×۹، ۰ = خالی
      solution: np.ndarray
      solved: bool
      difficulty: dict              # {"label", "emoji", "color"}
      solver_used: str              # "dlx" | "backtracking"
      solver_time_ms: float
      image_thumb: Optional[bytes] = None   # تصویر بندانگشتی PNG کوچک
      source: str = "upload"        # "upload" | "sample" | "generated" | "camera"

  class HistoryStore:
      def __init__(self, max_items: int = 20): ...
      def add(self, record: SolveRecord) -> None: ...
      def all(self) -> list[SolveRecord]: ...
      def get(self, id: str) -> Optional[SolveRecord]: ...
      def remove(self, id: str) -> None: ...
      def clear(self) -> None: ...
      def to_dict(self) -> dict: ...        # برای session_state
      @classmethod
      def from_dict(cls, d: dict) -> "HistoryStore": ...
  ```
- [ ] ذخیره‌ی تاریخچه در `st.session_state["history"]`
  - ⚠️ **مهم:** `session_state` در Streamlit بین ری‌استارت‌ها پایدار نمی‌ماند.
  - برای پایداری، به **اسپرینت ۲ #3+ (پایداری اختیاری)** در پایین نگاه کن.
- [ ] افزودن یک expander **«History»** در نوار کناری که نشان می‌دهد:
  - [ ] تعداد حل‌ها
  - [ ] فهرست N حل آخر با:
    - [ ] تصویر بندانگشتی (کوچک، ۴۰×۴۰)
    - [ ] نشان سختی
    - [ ] حل‌کننده‌ی استفاده‌شده
    - [ ] زمان
    - [ ] زمان نسبی («۲ دقیقه پیش»)
  - [ ] دکمه‌ی «Clear history»
  - [ ] کلیک روی یک ورودی → بارگذاری آن تخته در نمای فعلی
- [ ] پس از هر حل، `history.add(SolveRecord(...))` فراخوانی شود
- [ ] محدودیت ۲۰ آیتم (حذف FIFO)

**نگرانی‌های حافظه:**
- هر تصویر بندانگشتی ≤ ۵ کیلوبایت
- ۲۰ آیتم × (9×9 int + 9×9 int + ۵ کیلوبایت) ≈ ۲۰۰ کیلوبایت مجموع
- ✅ بدون فشار زیرساختی

**معیارهای پذیرش:**
- تاریخچه در طول یک جلسه پایدار است
- کلیک روی یک آیتم، آن پازل را دوباره رندر می‌کند
- پاک‌کردن تاریخچه کار می‌کند
- در حل‌های متعدد، نشتی حافظه وجود ندارد

---

# 🏃 اسپرینت ۲ — دوربین و PDF

**مدت:** ۲ تا ۳ روز
**هدف:** منبع ورودی جدید (دوربین) و قالب خروجی جدید (PDF).
**وابسته به:** اسپرینت ۱ (#3 تاریخچه)

---

## ⭐ #1 ورودی دوربین زنده

**چرا:** کاربران موبایل نمی‌توانند به‌راحتی فایل انتخاب کنند؛ دوربین این را مستقیم حل می‌کند.

**فایل‌هایی که باید تغییر کنند:**
- `app.py` — افزودن رابط تب‌دار
- `sudoku_solver/styles.py` — استایل تب دوربین

**جزئیات پیاده‌سازی:**
- [ ] افزودن رابط تب‌دار در بخش آپلود:
  ```python
  tab_upload, tab_camera = st.tabs(["📤 Upload", "📸 Camera"])
  ```
- [ ] در `tab_camera`، استفاده از `st.camera_input("Take a photo of the puzzle")`
- [ ] تبدیل `UploadedFile` برگشتی به همان فرمت آپلودر:
  - [ ] استفاده‌ی مجدد از منطق `np.frombuffer` + `cv2.imdecode`
  - [ ] نادیده‌گرفتن بهینه‌سازی سمت کلاینت (خروجی دوربین از قبل JPEG است)
- [ ] ردیابی منبع در `session_state` برای تاریخچه (`"source": "camera"`)
- [ ] افزودن یک راهنما: «برای بهترین نتیجه، گوشی را موازی با شبکه نگه دار»
- [ ] تست روی:
  - [ ] Chrome موبایل (اندروید)
  - [ ] Safari موبایل (iOS)
  - [ ] Chrome دسکتاپ

**مشکلات شناخته‌شده:**
- iOS Safari گاهی تصویر را می‌چرخاند → باید EXIF را تشخیص داد و در صورت نیاز چرخاند

**معیارهای پذیرش:**
- کاربر می‌تواند عکس بگیرد و بلافاصله حل کند
- روی موبایل و دسکتاپ کار می‌کند
- تصویر وارونه یا کج نیست
- قطعی WebSocket رخ نمی‌دهد (ورودی دوربین پایدار است)

---

## ⭐ #2 دانلود PDF (غنی‌سازی‌شده)

**چرا:** PNG خوب است، اما PDF می‌تواند زمینه‌ی بیشتری داشته باشد (تخته، حل، آمار، تاریخچه).

**فایل‌هایی که باید تغییر کنند:**
- `sudoku_solver/pdf_export.py` *(جدید)*
- `app.py` — دکمه‌ی دانلود
- `pyproject.toml` / `requirements.txt` — افزودن `reportlab`

**جزئیات پیاده‌سازی:**
- [ ] افزودن وابستگی `reportlab`
- [ ] ساخت `sudoku_solver/pdf_export.py` با `build_solution_pdf(record) -> bytes`
- [ ] ساختار PDF:
  ```
  ┌─────────────────────────────────────┐
  │   🧩 Sudoku Solution                │
  │   ─────────────────────────         │
  │   Date: 2026-09-23 14:32            │
  │   Difficulty: 🟠 Hard               │
  │   Solver: DLX (5.87 ms)             │
  │                                     │
  │   ┌───────────┐  ┌───────────┐     │
  │   │  Original │  │  Solved   │     │
  │   │  grid     │  │  grid     │     │
  │   └───────────┘  └───────────┘     │
  │                                     │
  │   Stats:                            │
  │   • Digits detected: 30/81          │
  │   • Cells solved: 51                │
  │   • Avg confidence: 98.7%           │
  │   • Time: 5.87 ms                   │
  │                                     │
  │   [Original photo thumbnail]        │
  │                                     │
  │   Generated by Sudoku ML            │
  │   github.com/satanic-kangaroo/...   │
  └─────────────────────────────────────┘
  ```
- [ ] پشتیبانی از **خروجی چندصفحه‌ای** (اگر تاریخچه چند رکورد دارد):
  - [ ] صفحه‌ی ۱ = جلد / خلاصه
  - [ ] صفحات ۲ تا N = هر پازل در یک صفحه
  - [ ] افزودن چک‌باکس در UI: «Include history (N puzzles)»
- [ ] رندر تخته در PDF: استفاده از `Table` در ReportLab با حاشیه‌های مناسب
- [ ] افزودن عکس اصلی (در صورت وجود) به‌عنوان یک تصویر بندانگشتی کوچک

**معیارهای پذیرش:**
- PDF با موفقیت دانلود می‌شود
- چیدمان هم در حالت تک‌صفحه و هم چندصفحه تمیز است
- متن سیریلیک/عربی (اگر بعداً اضافه شد) به‌درستی رندر می‌شود
- حجم فایل برای ۵ پازل کمتر از ۵۰۰ کیلوبایت

---

## 🧊 #3+ پایداری اختیاری (یخچال برای بعد)

**در این اسپرینت نیست** — فقط برای یادآوری:
- تاریخچه‌ی جلسه با ری‌استارت اپ از دست می‌رود
- اگر بعداً پایداری خواستیم:
  - گزینه‌ی A: SQLite در `~/.sudoku_ml/history.db`
  - گزینه‌ی B: حساب کاربری + ذخیره‌سازی ابری
  - گزینه‌ی C: خروجی/ورودی فایل JSON
- ⏳ بعد از اسپرینت ۳ دوباره بررسی شود

---

# 🏃 اسپرینت ۳ — تولیدکننده و یادگیری

**مدت:** ۱ هفته
**هدف:** تبدیل اپ از یک *ابزار* به یک *پلتفرم یادگیری*.
**وابسته به:** اسپرینت ۱ (تاریخچه)

---

## ⭐ #9 تولیدکننده‌ی پازل با امتیاز سختی

**چرا:** کاربران می‌توانند بی‌نهایت پازل تولید کنند؛ همچنین برای آموزش/تست حل‌کننده مفید است.

**فایل‌هایی که باید تغییر کنند:**
- `sudoku_solver/samples.py` — گسترش تولیدکننده‌ی موجود
- `sudoku_solver/difficulty.py` *(جدید)* — امتیازدهی علمی سختی
- `app.py` — گسترش بخش «Generate»

**جزئیات پیاده‌سازی:**

### بخش A: تولید هسته (تا حدی موجود است)
- [ ] تأیید اینکه `generate_random_puzzle_image()` پازل‌های **با جواب یکتا** تولید می‌کند
  - [ ] افزودن `has_unique_solution(board)` — DLX را دو بار با seedهای تصادفی متفاوت اجرا می‌کند و بررسی می‌کند آیا دو جواب وجود دارد
  - [ ] اگر یکتا نبود، آخرین خانه را دوباره سوراخ کن
- [ ] ریفکتور: جدا کردن `generate_solved_board()` از `punch_holes(board, n)`

### بخش B: امتیازدهی سختی
- [ ] ساخت `sudoku_solver/difficulty.py`:
  ```python
  def rate_difficulty(board) -> DifficultyScore:
      """
      امتیازدهی بر اساس چندین عامل:
      - تعداد اعداد داده‌شده
      - حل‌کننده‌ی backtracking: گره‌ها + بیشترین عمق
      - حل‌کننده‌ی DLX: گره‌ها
      - تکنیک‌های انسانی موردنیاز (اگر human_solver موجود باشد)
      """
  ```
- [ ] عوامل:
  - [ ] `givens_count` (بالاتر = آسان‌تر)
  - [ ] `backtracking_nodes` (بالاتر = سخت‌تر)
  - [ ] `max_recursion_depth`
  - [ ] `branching_factor_at_root`
  - [ ] (بخش دوم اسپرینت ۳) `human_techniques_required`
- [ ] ترکیب در یک امتیاز واحد از ۱ تا ۱۰
- [ ] نگاشت امتیاز به برچسب:
  - [ ] ۱–۳ = 🟢 آسان
  - [ ] ۴–۵ = 🟡 متوسط
  - [ ] ۶–۷ = 🟠 سخت
  - [ ] ۸–۹ = 🔴 حرفه‌ای
  - [ ] ۱۰ = ⚫ شیطانی

### بخش C: UI
- [ ] جایگزینی اسلایدر سوراخ فعلی با انتخاب‌گر «Difficulty target»:
  ```
  🟢 Easy  🟡 Medium  🟠 Hard  🔴 Expert  🎲 Random
  ```
- [ ] در پشت صحنه، تولید را ادامه بده تا سختی هدف حاصل شود (حداکثر ۱۰ تلاش)
- [ ] نمایش:
  - [ ] امتیاز سختی واقعی
  - [ ] تعداد اعداد داده‌شده
  - [ ] زمان تولید
- [ ] افزودن پیش‌نمایش «Show puzzle» قبل از حل

**معیارهای پذیرش:**
- پازل‌های تولیدشده همیشه دقیقاً یک جواب دارند
- برچسب‌های سختی با ادراک انسانی همخوانی دارند (اعتبارسنجی روی ۲۰ تست دستی)
- تولید حتی برای سخت‌ترین سطوح زیر ۳ ثانیه طول می‌کشد

---

## ⭐ #10 حل‌کننده‌ی انسانی / توضیح گام‌به‌گام

**چرا:** پرتقاضاترین قابلیت. اپ را به یک ابزار آموزشی تبدیل می‌کند.
**وابسته به:** #9 (امتیازدهی سختی چارچوب تکنیک‌ها را به اشتراک می‌گذارد)

**فایل‌هایی که باید تغییر کنند:**
- `sudoku_solver/human_solver.py` *(جدید)* — بخش بزرگ
- `sudoku_solver/techniques.py` *(جدید)* — پیاده‌سازی تکنیک‌های منفرد
- `app.py` — تب جدید «Learn»

**مفهوم:** مثل یک انسان حل کن، گام‌به‌گام، با استفاده از تکنیک‌های منطقی پیش از افتادن به دام حدس‌زدن.

### بخش A: کتابخانه‌ی تکنیک‌ها

این‌ها را به ترتیب پیچیدگی پیاده کن:

**سطح ۱ — پایه (همیشه لازم):**
- [ ] **Naked Single** — یک خانه فقط یک کاندیدا دارد
- [ ] **Hidden Single** — یک رقم فقط در یک خانه از یک سطر/ستون/جعبه جا می‌شود

**سطح ۲ — متوسط:**
- [ ] **Naked Pair** — دو خانه در یک واحد دقیقاً دو کاندیدا مشترک دارند
- [ ] **Hidden Pair** — دو رقم فقط در همان دو خانه ظاهر می‌شوند
- [ ] **Naked Triple** / **Hidden Triple**
- [ ] **Pointing Pair/Triple** — یک رقم در یک جعبه محدود به یک سطر/ستون می‌شود
- [ ] **Box-Line Reduction** — همان ایده به‌صورت معکوس

**سطح ۳ — پیشرفته:**
- [ ] **X-Wing** — الگوی ۲×۲ روی ۴ خانه
- [ ] **Swordfish** — توسعه‌ی ۳×۳
- [ ] **XY-Wing** — pivot + ۲ pincer
- [ ] **XYZ-Wing**

**سطح ۴ — حرفه‌ای:**
- [ ] **Unique Rectangle** انواع ۱ تا ۴
- [ ] **Coloring / Simple Colors**
- [ ] **XY-Chain**

**Fallback:**
- [ ] اگر هیچ تکنیکی اعمال نشد → حدس با backtracking
- [ ] این گام را به‌صورت بصری به‌عنوان «حدس» علامت بزن

### بخش B: موتور

- [ ] `human_solver.py`:
  ```python
  @dataclass
  class SolveStep:
      index: int
      technique: str           # "naked_single", "x_wing", ...
      cells_involved: list[tuple[int, int]]
      digits_eliminated: list[int]
      placement: Optional[tuple[int, int, int]]  # (r, c, digit)
      explanation: str         # متن قابل‌خواندن برای انسان
      board_before: np.ndarray
      board_after: np.ndarray
      candidates_before: dict  # {(r, c): set[int]}

  def solve_humanly(board) -> tuple[np.ndarray, list[SolveStep]]:
      """گام‌به‌گام حل کن و هر استنتاج را ثبت کن."""
  ```
- [ ] در طول مسیر مجموعه‌های کاندیدا را ردیابی کن
- [ ] ترتیب اولویت: ابتدا ارزان‌ترین تکنیک → اگر گیر کردی، تشدید کن

### بخش C: توضیحات

برای هر تکنیک، یک **توضیح انسان‌پسند** به زبان کاربر بنویس:

نمونه‌های خروجی:
```
گام ۱ — Naked Single
  خانه‌ی R4C5 فقط یک مقدار ممکن دارد: 8.
  همه‌ی ارقام دیگر (۱ تا ۷ و ۹) در سطر، ستون یا جعبه‌اش ظاهر می‌شوند.

گام ۲ — Hidden Single
  در سطر ۳، رقم ۴ فقط در خانه‌ی R3C7 جا می‌شود.
  هر خانه‌ی دیگری در این سطر از قبل ۴ را حذف کرده است.

گام ۳ — X-Wing روی رقم ۶
  رقم ۶ محدود است به R2C4، R2C8، R7C4، R7C8.
  این چهار خانه یک الگوی X می‌سازند → ۶ نمی‌تواند در جای دیگری
  از ستون‌های ۴ و ۸ ظاهر شود.
```

### بخش D: UI

- [ ] افزودن تب جدید **«📚 Learn»**
- [ ] نمایش تخته با:
  - [ ] خانه‌های هایلایت‌شده (رنگ‌بندی بر اساس نوع تکنیک)
  - [ ] نمایش کاندیداها (ارقام کوچک در گوشه‌ها)
- [ ] اسلایدر خط زمانی:
  - [ ] گام ۰ = پازل اولیه
  - [ ] گام N = حل نهایی
- [ ] دکمه‌های Play/Pause/گام جلو/گام عقب
- [ ] حالت پخش خودکار با سرعت قابل تنظیم
- [ ] نوار کناری نشان می‌دهد:
  - [ ] نام تکنیک فعلی
  - [ ] توضیح قابل‌خواندن برای انسان
  - [ ] فهرست تکنیک‌های استفاده‌شده‌ی قبلی همراه با تعداد
- [ ] آمار: «با ۱۲ گام و ۵ تکنیک حل شد»

**معیارهای پذیرش:**
- [ ] موتور بیش از ۹۰٪ پازل‌های منتشرشده‌ی سودوکو را فقط با منطق حل می‌کند
- [ ] هر گام به‌صورت دستی قابل تأیید است
- [ ] متن توضیحات برای یک مبتدی شفاف است
- [ ] انیمیشن روی موبایل روان اجرا می‌شود

---

# 🏃 اسپرینت ۴ — جذابیت

**مدت:** ۲ هفته
**هدف:** ویروسی‌کردن پروژه.
**وابسته به:** اسپرینت ۲ (#1 دوربین)

---

## ⭐ #8 بصری‌ساز DLX

**چرا:** DLX جادویی‌ترین الگوریتم اینجاست؛ بصری‌سازی آن مسحورکننده و آموزشی است.

**فایل‌هایی که باید تغییر کنند:**
- `components/dlx_visualizer/index.html` *(جدید)* — بصری‌ساز D3.js
- `components/dlx_visualizer/dlx_viz.js` *(جدید)*
- `sudoku_solver/dlx_solver.py` — افزودن انتشار رویداد
- `app.py` — تب جدید «Visualize»

**جزئیات پیاده‌سازی:**

### بخش A: ابزارگذاری روی DLX
- [ ] افزودن پارامتر اختیاری `event_log` به `_search()`
- [ ] انتشار رویدادها:
  - [ ] `choose_column(col_id, size)`
  - [ ] `try_row(row_id)`
  - [ ] `cover_column(col_id)`
  - [ ] `uncover_column(col_id)`
  - [ ] `dead_end()`
  - [ ] `solution_found()`
- [ ] رویدادها دیکشنری‌های سبک هستند — بدون سریال‌سازی سنگین

### بخش B: بصری‌ساز D3
- [ ] استفاده از **D3.js** با چیدمان force-directed یا grid
- [ ] رندر گره‌ها به‌صورت دایره، اتصالات به‌صورت خط
- [ ] ستون‌ها آبی، سطرها نارنجی
- [ ] انیمیشن cover/uncover:
  - [ ] گره‌ها محو می‌شوند و به کنار می‌پرند
  - [ ] خطوط به‌نرمی جمع می‌شوند
- [ ] اسکرابر خط زمانی در پایین
- [ ] کنترل سرعت (۰٫۲۵× تا ۴×)

### بخش C: همگام‌سازی با پایتون
- [ ] جریان رویدادها از طریق `st.components.v1.html`
- [ ] استفاده از `st.session_state` برای نگه‌داری لاگ رویدادها
- [ ] کامپوننت از یک JSON ثابت می‌خواند (بدون نیاز به پوش بلادرنگ)

### بخش D: پوشش
- [ ] افزودن دکمه‌ی «🎬 Visualize DLX» کنار بخش مقایسه
- [ ] باز کردن یک مودال / تب جدید با بصری‌ساز
- [ ] افزودن یک پاراگراف مقدمه که توضیح دهد چه اتفاقی می‌افتد

**معیارهای پذیرش:**
- برای ~۵۰۰ رویداد با ۶۰ FPS روان اجرا می‌شود
- روی موبایل کار می‌کند (ممکن است جزئیات کاهش یابد)
- کاربر بدون خواندن کد متوجه می‌شود چه اتفاقی می‌افتد

---

## ⭐ #7 پوشش AR

**چرا:** تک‌تأثیرگذارترین قابلیت. تقریباً هیچ اپ سودوکویی این را به‌صورت زنده ندارد.
**وابسته به:** #1 دوربین زنده

**فایل‌هایی که باید تغییر کنند:**
- `sudoku_solver/ar_overlay.py` *(جدید)*
- `app.py` — حالت جدید «AR»

**جزئیات پیاده‌سازی:**

### بخش A: پردازش فریم
- [ ] گرفتن فریم از `streamlit-webrtc` (یا `st.camera_input` در یک حلقه)
- [ ] کاهش مقیاس به ۶۴۰×۴۸۰ برای سرعت
- [ ] اجرای خط لوله‌ی تشخیص:
  - [ ] تشخیص شبکه
  - [ ] Warp
  - [ ] تشخیص رقم (دسته‌ای)
  - [ ] حل
- [ ] پوشش حل روی **فریم اصلی** (نه warp شده)

### بخش B: پایداری
- [ ] ردیابی گوشه‌های شبکه بین فریم‌ها (optical flow یا فیلتر کالمن)
- [ ] فقط زمانی دوباره حل کن که مجموعه‌ی ارقام تغییر کرده باشد
- [ ] نرم‌کردن موقعیت پوشش بین فریم‌ها

### بخش C: کارایی
- [ ] تبدیل مدل به TFLite برای استنتاج سریع‌تر (~۵× سریع‌تر)
- [ ] دسته‌بندی هر ۸۱ خانه در یک فراخوانی مدل
- [ ] کش آخرین تخته‌ی حل‌شده و استفاده‌ی مجدد در صورت عدم تغییر

### بخش D: UI
- [ ] تب جدید **«📸 Live»**
- [ ] نمایش فید ویدیو با پوشش
- [ ] شمارنده‌ی FPS
- [ ] دکمه‌ی «Snapshot and solve» (فریم را منجمد می‌کند)

**معیارهای پذیرش:**
- ≥ ۱۰ FPS روی یک گوشی میان‌رده
- پوشش قفل‌شده روی شبکه باقی می‌ماند
- با لرزش متوسط دست کار می‌کند

**ریسک‌های شناخته‌شده:**
- `streamlit-webrtc` شیب یادگیری دارد
- مجوزهای WebRTC در مرورگرهای موبایل متفاوت است

---

# 🏃 اسپرینت ۵ — محصول

**مدت:** ۲ هفته
**هدف:** تبدیل به یک محصول واقعی قابل عرضه.
**وابسته به:** همه‌ی اسپرینت‌های قبلی (معماری باید پایدار باشد)

---

## ⭐ #11 TFLite + PWA

**چرا:** قابلیت آفلاین، حریم خصوصی‌محور، قابل نصب روی گوشی.

**فایل‌هایی که باید تغییر کنند:**
- `scripts/export_tflite.py` *(جدید)*
- `pwa/` *(جدید)* — Progressive Web App
- `pwa/service-worker.js`
- `pwa/manifest.json`

**جزئیات پیاده‌سازی:**

### بخش A: خروجی TFLite
- [ ] اسکریپت: `scripts/export_tflite.py`
  ```python
  import tensorflow as tf

  model = tf.keras.models.load_model("digit_model.keras")
  converter = tf.lite.TFLiteConverter.from_keras_model(model)
  converter.optimizations = [tf.lite.Optimize.DEFAULT]
  # اختیاری: کوانتیزاسیون int8 (کوچک‌تر، سریع‌تر، کمی کم‌دقت‌تر)
  # converter.target_spec.supported_ops = [tf.lite.OpsSet.TFLITE_BUILTINS_INT8]

  tflite = converter.convert()
  open("models/digit_model.tflite", "wb").write(tflite)
  ```
- [ ] تأیید دقت روی مجموعه‌ی تست (هدف: ≥ ۹۹٪)
- [ ] مقایسه‌ی حجم: `.keras` در برابر `.tflite`

### بخش B: PWA
- [ ] انتقال استنتاج به **TensorFlow.js**:
  - [ ] تبدیل TFLite → TFJS با `tensorflowjs_converter`
  - [ ] بارگذاری در مرورگر و اجرای استنتاج
- [ ] انتقال خط لوله به JS:
  - [ ] تشخیص شبکه: **OpenCV.js**
  - [ ] Warp: معادل `cv2.getPerspectiveTransform`
  - [ ] حل‌کننده: پورت DLX به JS (یا برای سادگی از Backtracking استفاده کن)
- [ ] Service worker برای کش آفلاین
- [ ] `manifest.json` برای اعلان نصب

### بخش C: استقرار
- [ ] میزبانی روی GitHub Pages یا Vercel
- [ ] صفحه‌ی فرود با دکمه‌ی «Install app»
- [ ] تست جریان نصب روی:
  - [ ] Chrome اندروید
  - [ ] Safari iOS (از «Add to Home Screen» استفاده می‌کند)
  - [ ] Chrome دسکتاپ

**معیارهای پذیرش:**
- اپ کاملاً آفلاین کار می‌کند
- اعلان نصب ظاهر می‌شود
- استنتاج زیر ۵۰۰ میلی‌ثانیه روی گوشی میان‌رده اجرا می‌شود
- حجم مدل زیر ۵۰۰ کیلوبایت

---

# 🧊 یخچال — جامعه‌ی متن‌باز

**در برنامه‌ریزی اسپرینت نیست** — چشم‌انداز بلندمدت.

## توزیع
- [ ] پکیج PyPI (`pip install sudoku-ml`)
- [ ] ایمیج داکر (`docker run satanickangaroo/sudoku-ml`)
- [ ] رسپی Conda-forge
- [ ] Homebrew tap (مک‌اواس)

## مستندات
- [ ] سایت MkDocs Material
- [ ] مرجع API (تولیدشده‌ی خودکار از docstringها)
- [ ] مجموعه‌ی آموزشی («خودت در ۳۰ دقیقه بساز»)
- [ ] پست‌های وبلاگ عمیق درباره‌ی الگوریتم‌ها
- [ ] ویدیوهای راهنما

## CI/CD
- [ ] GitHub Actions:
  - [ ] Lint (`ruff`، `black`)
  - [ ] تست (`pytest` + coverage)
  - [ ] بررسی نوع (`mypy`)
  - [ ] استقرار خودکار روی Streamlit Cloud هنگام merge
  - [ ] انتشار خودکار روی PyPI هنگام tag
- [ ] هوک‌های pre-commit
- [ ] Dependabot

## جامعه
- [ ] `CONTRIBUTING.md`
- [ ] `CODE_OF_CONDUCT.md`
- [ ] `SECURITY.md`
- [ ] قالب‌های issue (باگ، قابلیت، سؤال)
- [ ] قالب PR
- [ ] فعال‌سازی Discussions
- [ ] برچسب‌های «Good first issue»
- [ ] تقدیر از مشارکت‌کنندگان (بات all-contributors)

---

# 🧊 یخچال — ایده‌های خارج از چارچوب

**برای بعد ذخیره شده. زمانی که هسته محکم شد، دوباره سراغش برو.**

## 🤖 مربی سودوکو
به‌جای حل کردن، کاربر را با راهنمایی هدایت کن:
- «به R4C5 نگاه کن. می‌توانی یک hidden single پیدا کنی؟»
- راهنمایی تدریجی (اشاره → نام تکنیک → حرکت دقیق)
- پیگیری پیشرفت و اشتباهات کاربر

## 🎮 نبرد سودوکو
- چندنفره: مسابقه با یک کاربر دیگر
- بلادرنگ بر پایه‌ی WebSocket
- سیستم امتیاز Elo
- جدول امتیازات

## 🧩 انواع سودوکو
- **سودوکو قطری** — هر دو قطر اصلی باید شامل ۱ تا ۹ باشند
- **سودوکو نامنظم** — ۹ ناحیه‌ی غیرمستطیل
- **سودوکو قاتل (Killer)** — نواحی دارای قید مجموع
- **سودوکو سامورایی** — ۵ شبکه‌ی همپوشان
- **سودوکو ترمومتر** — دماسنج‌ها
- **سودوکو فلش** — فلش‌هایی با مجموع
- **چند-سودوکو** — ۳ تا ۹ شبکه کنار هم

**نکته:** معماری DLX از قبل از انواع پشتیبانی می‌کند؛ فقط ستون‌های قید باید تغییر کنند.

## 📊 آزمایشگاه تحقیقاتی سودوکو
- بنچمارک دسته‌ای روی ۱۰۰۰+ پازل
- تحلیل آماری کارایی حل‌کننده
- مدل ML برای طبقه‌بندی سختی پازل
- تولید گزارش «وضعیت حل‌کننده‌های سودوکو»
- پشتیبانی از واردکردن از مجموعه‌داده‌های شناخته‌شده (Norvig، Kaggle)

## 🎨 هنر سودوکو
- یک تصویر بگیر (عکس، لوگو)
- پازل سودوکویی تولید کن که جوابش آن تصویر را بسازد
- بسیار مناسب اینستاگرام

## 🌍 بومی‌سازی
- [ ] فارسی (fa)
- [ ] عربی (ar)
- [ ] فرانسوی (fr)
- [ ] اسپانیایی (es)
- [ ] آلمانی (de)
- [ ] چینی (zh)
- [ ] ژاپنی (ja)
- پشتیبانی RTL برای عربی/فارسی

## 🔌 REST API
- بک‌اند FastAPI
- Endpointها:
  - `POST /solve` — تصویر → جواب
  - `POST /generate` — سختی → پازل
  - `POST /explain` — پازل → گام‌ها
  - `GET /benchmark` — اجرای بنچمارک
- رابط Swagger
- محدودسازی نرخ
- کلیدهای API

## 📚 کتاب‌کار PDF دسته‌ای
- آپلود یک PDF شامل ۵۰ پازل
- حل همه
- خروجی یک PDF جدید با جواب‌های روی‌هم‌افتاده
- مفید برای معلمان، ناشران

## 📱 اپ‌های موبایل بومی
- پوشش React Native یا Flutter
- استفاده از همان مدل TFLite
- یکپارچگی بومی دوربین
- اعلان‌های پوش (پازل روزانه)

---

# 📝 لاگ تصمیم‌ها

تصمیم‌های مهم را اینجا ثبت کن تا «خودِ آینده» دلیلش را بفهمد.

| تاریخ | تصمیم | دلیل |
|------|----------|--------|
| ۲۰۲۶-۰۹-۲۲ | استفاده از کامپوننت سفارشی Streamlit برای آپلودر | رفع قطعی WebSocket موبایل + کاهش ۹۵٪ حجم payload |
| ۲۰۲۶-۰۹-۲۲ | استفاده از DLX به‌عنوان حل‌کننده‌ی پیش‌فرض، Backtracking به‌عنوان fallback | DLX روی پازل‌های سخت ۱۰۰ تا ۲۰۰ برابر سریع‌تر است |
| ۲۰۲۶-۰۹-۲۲ | مجموعه‌داده‌ی مصنوعی به‌جای MNIST | ارقام سودوکو چاپی هستند، نه دست‌نویس |
| ۲۰۲۶-۰۹-۲۳ | فعلاً صرف‌نظر از PyPI | تمرکز روی قابلیت‌های کاربر؛ بسته‌بندی بعداً |
| ۲۰۲۶-۰۹-۲۳ | تاریخچه فقط در جلسه (بدون پایداری) | سادگی زیرساخت؛ در اسپرینت ۳ دوباره بررسی شود |

---

# 🎯 تمرکز فعلی

**اسپرینت:** ۱ — بردهای سریع
**وظیفه‌ی بعدی:** #4 کپی تخته به‌صورت متن
**زمان تخمینی:** ۳۰ دقیقه

**موارد مسدود:** هیچ

**یادداشت‌ها:**
- یادت باشد پس از هر اسپرینت این فایل را به‌روزرسانی کنی
- اسپرینت‌های تمام‌شده را در `CHANGELOG.md` بایگانی کن
- `README.md` را با قابلیت‌های جدید همگام نگه دار

---

*آخرین به‌روزرسانی: ۲۰۲۶-۰۹-۲۳*